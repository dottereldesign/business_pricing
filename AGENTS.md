# Project deployment

- After completing and verifying any project update, commit the intended changes and push them directly to `origin/main` so the update is reflected on GitHub Pages.
- Do not leave completed updates only in the local working tree unless the user explicitly asks not to publish them.
- If a push fails, report the failure clearly and leave the local commit intact for recovery.
